<LabelAutoContrast>:
    mipmap: True
    markup: True


<LabelHighlight>:
    mipmap: True
    width: self.texture_size[0]
    height: self.texture_size[1]
    text_size: self.width, None
    markup: True
    font_family: app.fonts['mono'] if self.highlight else app.fonts['default']
