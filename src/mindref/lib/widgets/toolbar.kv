#:import ExpandingLabel mindref.lib.widgets.style
#:import buttons mindref.lib.widgets.buttons
<Toolbar>:
    orientation: 'horizontal'
    padding: 0
    canvas.before:
        Color:
            rgba: app.colors['Primary']
        Rectangle:
            size: self.size
            pos: self.pos
    OpenMenuButton:
        enable_ripple_effect: False
        on_release: app.menu_open = not app.menu_open
        icon_size: app.base_font_size * 2
        size_hint: None, None
        width: self.height
        padding: 0
        border: 0


    ExpandingLabel:
        text: app.APP_NAME
        halign: 'center'
        valign: 'center'
        font_size: app.base_font_size + sp(2)

